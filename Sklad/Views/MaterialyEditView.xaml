<Window x:Class="Sklad.Views.MaterialyEditView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Sklad.Views"
        xmlns:vm="clr-namespace:Sklad.ViewModels"
        mc:Ignorable="d"
        Title="Výběr materiálu" Height="450" Width="800"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DataGrid Grid.Row="0" ItemsSource="{Binding MaterialyCol}" 
                  SelectedItem="{Binding SelectedMaterial}"
                  AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single" Margin="5">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding MaterialId}" Width="Auto"/>
                <DataGridTextColumn Header="Název" Binding="{Binding Nazev}" Width="*"/>
                <DataGridTextColumn Header="Měrná jednotka" Binding="{Binding MernaJednotka.Popis}" Width="Auto"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="5">
            <Button Content="Vybrat" Command="{Binding ConfirmCommand}" Width="100" Margin="5" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
